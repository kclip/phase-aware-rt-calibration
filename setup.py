from setuptools import setup, find_packages

setup(
    name="phase-aware-rt-calibration",
    packages=find_packages(),
    # entry_points='''
    #     [console_scripts]
    #     cli=cli:cli
    # ''',
)
